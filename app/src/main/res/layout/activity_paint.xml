<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/print_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.trd.screendrawing.PaintView
        android:id="@+id/paint_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="32dp"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/image_dialog_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:backgroundTint="@android:color/transparent"
            android:gravity="bottom"
            android:src="@drawable/ic_emoji"
            android:text="open images" />

        <ImageButton
            android:id="@+id/open_camera_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:backgroundTint="@android:color/transparent"
            android:gravity="bottom"
            android:src="@drawable/ic_camera"
            android:text="open camera" />


        <Spinner
            android:id="@+id/colorSpinner"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="0.9"
            android:scrollbars="none"
            android:background="@null"
            android:foregroundGravity="center_vertical"
            android:gravity="bottom"
            android:popupBackground="@drawable/spinner_background_rounded"
            android:spinnerMode="dropdown"
            android:visibility="visible"
            tools:background="@drawable/ic_color" />

        <ImageButton
            android:id="@+id/clear_screen_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:backgroundTint="@android:color/transparent"
            android:gravity="bottom"
            android:src="@drawable/ic_delete"
            android:text="clear screen" />

    </LinearLayout>

</RelativeLayout>

